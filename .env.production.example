# ============================================
# Databricks Sword — Production Environment
# ============================================
# Copy these variables to Dokploy → Environment tab.
# Lines starting with # are comments and will be ignored.

# ===================================
# DOMAIN (Required)
# ===================================
# The public domain where the app is served.
# Used in Traefik labels for routing. Create an A record pointing here.
DOMAIN=databricks.preprabbit.in

# ===================================
# PUBLIC SITE URL (Required)
# ===================================
# Full URL including https:// — used for sitemap, OG tags, auth callbacks
NEXT_PUBLIC_SITE_URL=https://databricks.preprabbit.in

# ===================================
# DATABASE — Turso (Required)
# ===================================
# Remote SQLite via Turso. Get from: https://turso.tech/app
TURSO_DATABASE_URL=libsql://your-db-name.turso.io
TURSO_AUTH_TOKEN=your-turso-auth-token

# ===================================
# AUTHENTICATION — NextAuth v5 (Required)
# ===================================
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-auth-secret-here

# Trust the host header behind Traefik proxy (keep true for Dokploy)
AUTH_TRUST_HOST=true

# Google OAuth — https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: https://databricks.preprabbit.in/api/auth/callback/google
AUTH_GOOGLE_ID=your-google-client-id
AUTH_GOOGLE_SECRET=your-google-client-secret

# GitHub OAuth — https://github.com/settings/developers
# Authorization callback URL: https://databricks.preprabbit.in/api/auth/callback/github
AUTH_GITHUB_ID=your-github-client-id
AUTH_GITHUB_SECRET=your-github-client-secret

# ===================================
# DATABRICKS PAT ENCRYPTION (Optional)
# ===================================
# AES-256 encryption key for storing Databricks Personal Access Tokens.
# Only needed if Databricks workspace integration is enabled.
# Generate with: openssl rand -hex 32
AES_ENCRYPTION_KEY=your-64-char-hex-key-here

# ===================================
# ADMIN PANEL (Required for content management)
# ===================================
# Password for accessing /admin to manage blog and FAQ content
# Use a strong unique password
ADMIN_PASSWORD=your-secure-admin-password
