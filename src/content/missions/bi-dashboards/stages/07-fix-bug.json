{
  "description": "A Lakeview filter parameter is wired to the wrong field. Fix the WHERE clause so :region filters store_region.",
  "starterCode": "SELECT * FROM gold.sales_facts WHERE sale_date >= :region;",
  "expectedPattern": "where\\s+store_region\\s*=\\s*:region\\b",
  "simulatedOutput": "Issue: Changing the Region dashboard parameter does not change regional results.",
  "hints": [
    "Region parameters should filter the region column, not dates.",
    "Keep the parameter name as :region.",
    "Only the predicate needs to be corrected."
  ]
}
