{
  "description": "A data quality rule is inverted and quarantines valid purchases. Fix the valid_amount expectation so only rows with amount greater than zero pass.",
  "starterCode": "CONSTRAINT valid_amount EXPECT (amount <= 0)",
  "expectedPattern": "constraint\\s+valid_amount\\s+expect\\s*\\(\\s*amount\\s*>\\s*0\\s*\\)",
  "simulatedOutput": "Issue: 92% of rows quarantined unexpectedly.",
  "hints": [
    "Positive purchase amounts should pass.",
    "Check the comparison operator.",
    "Keep the same constraint name."
  ]
}
