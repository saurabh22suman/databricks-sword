{
  "description": "A JOIN uses the wrong key and drops valid customer-order matches. Fix the ON clause to join customers and orders by customer_id.",
  "starterCode": "SELECT c.name, o.order_id FROM customers c INNER JOIN orders o ON c.customer_id = o.order_id",
  "expectedPattern": "on\\s+c\\.customer_id\\s*=\\s*o\\.customer_id",
  "simulatedOutput": "Issue: Customer order counts are far lower than expected.",
  "hints": [
    "Join customer dimension to orders fact on customer_id.",
    "order_id should not be matched to customer_id.",
    "Only the join predicate needs correction."
  ]
}
